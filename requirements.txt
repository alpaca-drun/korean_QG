fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# LLM API 클라이언트
google-generativeai>=0.8.0  # 구조화된 출력 및 파일 업로드 지원을 위해 최신 버전 사용
openai==1.3.0

# 비동기 HTTP 클라이언트
httpx==0.25.0

# AWS SDK (SES 이메일 전송)
boto3==1.34.0

# 비동기 작업 처리 (선택사항)
celery==5.3.4
redis==5.0.1

# 데이터베이스
pymysql==1.1.0

# JWT 인증
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1

# test

python-docx==1.2.0