# 데이터베이스 초기화 스크립트

이 디렉토리의 SQL 스크립트는 MariaDB 컨테이너 **최초 실행 시에만** 자동으로 실행됩니다.

## 🔒 안전성

- ✅ **최초 1회만 실행**: 데이터가 이미 있으면 자동으로 건너뜁니다
- ✅ **재실행 안전**: 컨테이너를 재시작해도 스크립트가 다시 실행되지 않습니다
- ✅ **데이터 보존**: 기존 데이터는 절대 삭제되지 않습니다

## 📁 파일 목록

- `00-init-db.sql` - 기본 설정 (타임존, 문자셋)
- `01-schema.sql` - 테이블 스키마 (자동 생성됨)

## 🔄 실행 순서

파일은 이름 순서대로 실행됩니다:
1. `00-init-db.sql` → 기본 설정
2. `01-schema.sql` → 테이블 생성

## ⚠️ 완전 초기화가 필요한 경우

데이터를 완전히 삭제하고 처음부터 다시 시작하려면:

```bash
# 주의: 모든 데이터가 삭제됩니다!
docker-compose -f docker-compose.db.yml down -v
docker-compose -f docker-compose.db.yml up -d
```

이 경우에만 초기화 스크립트가 다시 실행됩니다.

## 📝 스키마 업데이트

스키마를 변경한 경우, 현재 DB에서 다시 export:

```bash
docker exec KG_db sh -c 'mysqldump -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" --no-data "$MYSQL_DATABASE"' > docker/mariadb/init/01-schema.sql
```
